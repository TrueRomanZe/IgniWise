# Modelos ML

Archivo principal:
- `random_forest_v1.pkl` - Modelo entrenado

El modelo se descarga desde Zenodo la primera vez que GitHub Actions se ejecuta.
Luego se guarda aquí para reutilizar en predicciones posteriores.
